spring.application.name=test-jwt-methods
server.port=8080

# Database connection settings for PostgreSQL (using default host, port, and database name)
spring.datasource.url=jdbc:postgresql://localhost:5432/springboot_test
spring.datasource.username=postgres
spring.datasource.password=bhavesh@27

# Hibernate settings for JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Email settings (Gmail SMTP details)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kaushikbhavesh20@gmail.com
spring.mail.password=nzmd jhyi otwt qyje
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Kafka settings (using local Kafka instance)
spring.kafka.producer.bootstrap-server=localhost:9092
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer

# Multipart file upload size limits
spring.servlet.multipart.max-file-size=3MB
spring.servlet.multipart.max-request-size=3MB
